<div><h2>Create a new list </h2></div>
	<p></p>
	<%= simple_form_for(:list, :url => {:action => 'create'},:html => {:class => 'form-horizontal' }) do |f| %>

		<%= render(:partial => "form", :locals => {:f => f}) %>
		<div class="bottonRow">
			<%= submit_tag("Create a New List")%>
		</div>
	<% end %>